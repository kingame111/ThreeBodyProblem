package org.example;

public class Equations {
Variables body [] = new Variables[3];
final double G = 6.67430*Math.pow(10, -11);

public double distance (Variables a, Variables b){
    double total =  Math.pow(a.getX()- b.getX(),2) + Math.pow(a.getY()- b.getY(),2) Math.pow(a.getZ()- b.getZ(),2);
   total = Math.sqrt(total);

}
public double gravity(Variables x, Variables y){
        double size =


    }

}
